# 1CallFix — Current Master Checkpoint

**This document supersedes all previous checkpoints.** It reflects the repository as of this session. Where it conflicts with `PROJECT_CURRENT_STATE.md`, this document is more current for the specific items it covers; `PROJECT_CURRENT_STATE.md` remains authoritative for everything else until it is next synced.

Labels: **IMPLEMENTED** shipped and wired · **VERIFIED** confirmed by an automated test this session · **PARTIAL** exists but incomplete · **MISSING** does not exist · **BUSINESS DECISION** needs a human call, not an engineering one · **UNREACHABLE** code that would be dead given current guards elsewhere.

---

## 1. Git state

- **HEAD:** the "FINAL EOD AUTONOMOUS COMPLETION MISSION" is now COMPLETE — all 20 mission phases done, from baseline `6b7c36e` (306/306) through Phase 20 (final release-readiness audit), all local to `main`, none pushed/deployed. Phase 20 is audit/documentation-only, no code changes, so no test-count movement. **Since then, Phase 21 (Release Candidate Hardening Backlog) is in progress** — items DOC-1, TECH-1 through TECH-4, TECH-6, and TECH-7 are all now complete (see commit-log entries 21–31 below); TECH-6 (Admin UI design system) reached COMPLETE with its fourth increment — all 35/35 real Livewire admin screens migrated, closing the last open technical item in this backlog. **BD-8's technical-preparation half is now also complete** (commit `1348057`, entry 32 below) — real SMS/push adapters exist, config-driven, none bound by default; the vendor decision + real credentials remain OPEN. HEAD is now at `1348057`, still none pushed/deployed.
- **Production:** unchanged at `ba0635a` throughout.
- **Full suite progression this segment:** 306/306 → 340/340 (Performance Campaigns) → 384/384 (KYC engine) → 406/406 (Compensation) → 422/422 (Chat) → 436/436 (Documents) → 458/458 (Notification Center) → 480/480 (Payment Accounts) → 491/491 (Operations expansion) → 505/505 (Admin Menu/Settings audit) → 523/523 (CMS/content audit) → 532/532 (Glover/6amMart parity audit) → 571/571 (Master Catalog Import) → 583/583 (Financial reconciliation audit) → 616/616 (API/security hardening) → 616/616 (international readiness audit — no code changes) → 620/620 (performance/scale audit) → 620/620 (final admin capability matrix — no code changes) → 620/620 (final release-readiness audit — no code changes). 0 failures/errors/warnings at every checkpoint.
- **Full suite progression, Phase 21 (current):** 620/620 → 620/620 (DOC-1, docs only, no code changes) → 629/629, 1494 assertions (TECH-1, Payouts row-level scope) → 645/645, 1515 assertions (TECH-2, currency symbol) → 668/668, 1546 assertions (TECH-3, timezone display) → 687/687, 1578 assertions (TECH-4, admin chat viewer) → 687/687 (documentation sync, no code changes) → 690/690, 1585 assertions (TECH-7, campaign audience chunking) → 714/714, 1656 assertions (TECH-6, admin UI design system first increment) → 726/726, 1694 assertions (TECH-6, admin UI design system second increment) → 739/739, 1741 assertions (TECH-6, admin UI design system third increment) → 747/747, 1759 assertions (TECH-6, admin UI design system fourth increment — FINAL, 35/35) → **778/778, 1799 assertions (BD-8, real SMS/push adapters — technical preparation, no vendor bound — current baseline).** 0 failures/errors/warnings at every checkpoint.

## 2. This segment's commits

1. **`d1e7995` — Performance/Growth Campaign Engine.** Configurable audience(Franchise/Provider/FieldWorker/Customer)/scope/metric/qualification(threshold or top-N)/reward-type architecture. Metrics computed from real `Booking.status='completed'` data only. Reward disbursement reuses Wallet/Loyalty/Badge — no parallel payout mechanism. Approve→disburse separated into its own permission (separation of duties). 34 tests.
2. **`ac816a8` + `3430197`(docs) — KYC completeness engine.** Document security overhaul (closed a real raw-`<a href>` public-link exposure), configurable required-document list per actor/country, Partner verification video, 30-day deadline with idempotent reminders, live-derived (never-boolean) withdrawal restriction enforced at `PayoutService::request()`, franchise-raises/central-admin-decides support-request workflow creating auditable time-bound exceptions. 44 tests.
3. **`cd174cc` — Tips/waiting/rain/overtime/peak/night compensation.** Overtime/night/peak auto-computed from real booking timestamps; rain/waiting admin-triggered only (no real data source exists to auto-derive them); tips move money customer-wallet→provider-wallet atomically. Every rate defaults to 0 (no invented values). Hooked into `CompleteBookingAction`. 22 tests.
4. **`a0b7b15` — Universal Chat.** `chat_messages` existed since P2 but was fully dormant. Built `ChatService` (participant-derivation + IDOR guard), `ChatController` (Sanctum API), private authorization-gated attachments. Supports Customer↔Partner, Customer↔Worker, Partner↔Worker — all three combinations naturally derived from real booking data. 16 tests.
5. **`447ea55` — Printing/Document Engine (user-directed continuation).** No PDF library existed anywhere in the repo; added `barryvdh/laravel-dompdf` per explicit user choice. One normalized view-model + one Blade template renders invoices/receipts for all three real Payment purposes (booking/wallet_topup/plan_subscription). Idempotent per-country-per-year numbering (row-locked sequence). Admin download reuses `payments.view`'s existing row-level scope; customer self-service API endpoint checks real payer ownership. 14 tests.
6. **`642d582` — Notification/Communication Center completeness (user-directed continuation).** Templates CRUD, Delivery Logs browser, Provider Status panel (adapter binding visibility, never exposes credentials), a real working `resendToFailedRecipients()` retry, and the in-app notification read API (Laravel's `database` channel had zero read-side despite being fully write-wired). All were real gaps where backend/permissions already existed but no UI/API consumed them. 22 tests.
7. **`8d44153` — Payment Admin completion (user-directed continuation).** `payment_accounts` was already read by PayoutService/Payouts\Manage but had zero write path anywhere — added `PaymentAccountService` (create/update/setDefault/delete/verify, verification always admin-only), self-service API, admin verification UI on the existing Payouts screen, and closed an IDOR-adjacent gap where a payout's payment_account_id was never checked to actually belong to the payee. `payment_methods` (the other table risk-register item 11 names) stays deliberately blocked on its own consolidation decision. 22 tests.
8. **Operations/Troubleshoot expansion (mission Phase 10).** `/admin/operations` previously only showed failed jobs, notification failures, and static health checks. Added: `payment_webhook_logs` (every Razorpay webhook receipt persisted regardless of outcome — an invalid signature or unmatched order used to vanish into a `Log::warning()` line with a 200 telling Razorpay to stop retrying) plus an admin "reprocess" action reusing the same idempotent `RazorpayWebhookHandler` extracted from `PaymentController`'s former private methods; `scheduled_task_runs` real run-history via `ScheduleRunTracker` wired onto all 4 existing `Schedule::command()` entries (no row means "hasn't run," never a fabricated "healthy"); three read-only detection services — `ReconciliationService` (paid-without-captured-payment, completed-without-commission, wallet ledger drift), `DispatchHealthService` (stale offers, exhausted bookings, reusing the existing `dispatch_attempts` table), `StuckBookingService` (Setting-driven per-status thresholds against `booking_status_history`) — all surfaced on the same screen, row-scoped via the same `AuthorizationService::scopeQuery` convention every other admin screen uses; and `ActivityLog` (existed since Phase P3, zero writers) now wired into every Operations mutation (job retry/discard, webhook reprocess). 11 new tests plus additive `PaymentWebhookLog` assertions on existing webhook tests. Full suite 491/491.
9. **Admin Menu/Settings completeness audit (mission Phase 11).** A full forensic sweep of every admin route/menu-item/permission/Setting key found: (a) **12 Livewire screens with NO view-level permission check at all** (Banners, Categories, Cms, Geography, Payouts, Roles, Subcategories, Franchises, FranchisePricing, Services, Settings, Zones) — same bug class addendum #1 fixed for 15 other screens, still open here; any admin-panel actor of any role could browse Payouts and the entire Roles & Permissions RBAC matrix. Fixed by gating each `mount()` with its own existing `.manage` permission (none had a separate `.view` slug). (b) **The sidebar rendered all 26 items unconditionally for every role** — now filtered per-item by the same permission each target screen enforces. Categories/Subcategories also promoted to top-level sidebar entries (previously reachable only via a secondary tab on Services). (c) **`settings.manage` was enforced on only 3 of 10 real Settings tabs** — closed by the new mount()-level gate above, which now covers all ten implicitly. (d) **27 real Setting keys** (KYC withdrawal/video/upload-size gates, all 10 Compensation rates/windows, 5 Auth/OTP parameters, 6 Operations detection thresholds, referral pending-expiry, subscription grace period) were read by already-wired application code with zero admin UI, permanently stuck on their hardcoded fallback default — 5 new/expanded Settings tabs added (KYC, Compensation, Security/OTP, Operations, Subscriptions — the last replacing a stale "zero consumers" placeholder). Security/OTP, Operations, and Subscriptions are correctly global-only in the new UI (their consumers pass no scope at all) rather than offering a scoped override the consumer would silently never read. (e) **`TipService`** (built Phase 5, full ownership/idempotency guards) had zero callers anywhere — added `POST /api/bookings/{id}/tip`. (f) Corrected a doc/reality mismatch: Universal Chat was listed on the "admin capability snapshot" table but has no admin screen at all (customer/partner-facing API only) — flagged as a new gap (risk register item 15) rather than silently built. `partner.workers.assign`'s missing permission check was investigated and deliberately left alone — the sibling permissions' own seeding comment shows they're for an unbuilt Partner-facing authorization layer, not the admin RBAC system; adding a check would either lock out real providers or require inventing a policy (risk register item 16). 22 new/updated tests (12 screens × view-gate coverage folded into the existing `ScreenViewAuthorizationTest`, `AdminSidebarVisibilityTest`, `SettingsManageTest`, 6 pre-existing RBAC test files updated for the new mount()-level 403, 3 new Tip endpoint tests). Full suite 505/505.
10. **CMS/content audit (mission Phase 12).** `Cms\Manage` (Pages/FAQs) and `Banners\Manage` were both fully admin-manageable but had ZERO consumer anywhere in the codebase — confirmed by a full-codebase read-site grep, zero matches in `routes/api.php`/`routes/web.php` for anything pages/faq/banner-related. Added a real public read API (`App\Http\Controllers\API\ContentController`, unauthenticated by design — same reasoning as `/auth/otp/*`): `GET /api/pages/{slug}`, `GET /api/faqs` (optional `?category=`), `GET /api/banners?placement=` (reuses `Banner::scopeForSlot()`, whose own docblock already named "customer app API" as an intended caller). `content_pages` gained an `is_active` column matching its sibling `faqs` table — previously a page went live the instant it was saved, which was inert with no public consumer but became consequential the moment one exists; the admin screen got a matching Published/Draft toggle. Investigated but deliberately NOT built: real Terms & Conditions/Privacy Policy content (writing legal text is a legal/business decision, not an engineering gap — a fabricated legal document would be worse than an honest gap) and multi-language support (single-locale throughout the content layer, `users.preferred_language` confirmed dead/unread schema — an architecture-wide decision spanning far more than CMS). Both logged as new risk-register items (17, 18) rather than invented or silently ignored; a third (19) notes a narrow, currently-inert image-orphan edge case on soft-deleted `Service` rows. 18 new tests (`CmsManageTest` — updatePage/editFaq/updateFaq/toggle actions/successful deletes/slug validation, none of which had coverage before; `ContentApiTest` — published/draft visibility, FAQ category filtering, banner targeting via the public API). Full suite 523/523.
11. **Glover/6amMart parity audit (mission Phase 13).** Found that real reference material actually exists on disk (outside this git repo) — full Glover Laravel source + DB dump, a full 6amMart admin panel, and, most significantly, a real prior-version 1CallFix production database (`DB_1cal_app_1.8.10.sql`) — correcting an earlier session's `GLOVER_VS_1CALLFIX_AUTH_AUDIT.md`, which had explicitly found nothing accessible. Full write-up in the new `GLOVER_6AMMART_PARITY_AUDIT.md`. Closed one concrete gap the audit found, matching the established "wire the orphaned code" pattern: **`reviews`** had existed since Phase 1 — real schema, model, and a `ReviewObserver` that recomputes `providers.rating_avg` — with zero `Review::create()` call sites anywhere (independently confirmed by the 6amMart comparison, which has a live review pipeline). Added `ReviewService` (submit/reply, one review per completed booking via a real DB unique constraint) and `ReviewController` (`POST /api/bookings/{id}/review`, `POST /api/reviews/{id}/reply`); `Providers\Show` now shows real review rows alongside the `rating_avg` figure that's been on that screen since Phase 1 with nothing behind it. Everything else stayed a documented finding, not a unilateral decision — real historical values found in the 1.8.10 dump (₹10 referral, 20%/12% commission splits, Firebase+MSG91 as the actual prior SMS/push vendor selection, Razorpay-only confirmed as deliberate precedent, `payment_methods`-as-sole-source-of-truth precedent, unified-RBAC-for-all-actors precedent) were added as evidence notes to the relevant `KNOWN_RISKS_AND_DECISIONS.md` items (1, 5, 6, 7, 8, 9, 11, 12, 13, 14, 15, 16, 17, 18) without changing any code default. 9 new tests (`ReviewTest`). Full suite 532/532.
12. **Master Catalog Import + Operations Import/Export completeness (mission Phase 14).** User-supplied real historical catalog spreadsheets plus real screenshots of the reference product's live `/central/operations/{imports,exports,backup}` screens. Found the existing Categories/Subcategories/Services import (built in an earlier, unlogged session) had real, working Upload→Validate→Preview→Commit UI but **zero test coverage** and reused a source file's `id` column directly as the real database primary key — exactly the practice the new spec explicitly forbids, since it means a future import could collide with or overwrite an unrelated real row. Rebuilt around a new shared engine, `App\Services\Catalog\CatalogImporter` (+ `CategoryImporter`/`SubcategoryImporter`/`ServiceImporter`): a new nullable-unique `external_id` column on all three catalog tables decouples the real PK from any source system's identity — row matching is external_id-only, and relationship columns (category_id/subcategory_id) resolve via a two-step external_id-then-real-id lookup that correctly handles both a fresh historical import and a round-trip re-export/re-import of 1CallFix's own catalog. Every commit is wrapped in one DB transaction (genuine rollback-on-partial-failure) and persists a new `catalog_import_runs` audit row (created/updated/unchanged/deactivated/skipped/failed counts + full per-row JSON detail) — a real Import Report, not an ephemeral message. Deactivate-missing is opt-in and only ever touches externally-sourced rows, never a manually-created one. `vendor_Type_id` 5/9 (confirmed real historical values) map to the existing `service`/`commerce` `App\Support\Modules` slugs; an unrecognized value is a validation error, never a guessed default. Investigated Vendors/Menus/Products import (the reference product's own Data Import screen has them) and a database-backup screen (the reference product's own Data Backup screen) — both deliberately NOT built (no target schema for the former; a "download the full production DB" feature is a real exfiltration risk to improvise without knowing the real deployment environment for the latter) — logged as new risk-register items (20, 21) instead. Added real Earnings/Payouts exports (`CommissionsExport`/`PayoutsExport`, matching the reference product's own Data Export screen) reusing existing ledger data as source of truth, never recalculated, with sensitive banking fields (account_number/ifsc) masked to last-4 only — found and logged (not silently fixed) that `Payouts\Manage` itself has no row-level franchise scoping, unlike `Commissions\Index` (risk register item 22). 39 new tests across `tests/Feature/Catalog/*` (importer engine, relationship resolution, idempotent re-import, rollback-on-partial-failure, authorization/global-scope-restriction, real end-to-end CSV upload) and `tests/Feature/Operations/DataExportTest.php` (row-level export scope, sensitive-field exclusion, financial-value correctness). Full suite 571/571.
13. **Financial reconciliation audit (mission Phase 15).** Audited every money-moving path this mission has built — campaigns, compensation, tips, referrals, payouts, loyalty, wallet top-ups — for real financial-integrity gaps rather than guessing at candidates. Found and fixed one genuine race: `LoyaltyService::redeem()` checked its unlocked `SUM()` balance *before* opening its transaction — two concurrent redemptions for the same user could both read a sufficient balance and both succeed, driving the aggregate ledger negative. Fixed by moving the check inside the transaction, behind a `lockForUpdate()` on the user's own ledger rows — the exact same guarantee `WalletService` already gives `wallets.balance`, just applied to the ledger-only loyalty balance for the first time. Extended `ReconciliationService` (Phase 10) with two new read-only detection checks, both surfaced on the existing Operations/Troubleshoot screen: wallet top-ups Razorpay captured but never credited (`RazorpayWebhookHandler`'s Payment-capture and wallet-credit are two separate, non-atomic transactions — a process death between them leaves real captured money with no matching `WalletTransaction` ever landing) and negative aggregate loyalty balances (the detection half of the race above, catching anything that slipped through before the fix or via a future bypass). Campaigns/Compensation/Tips/Referrals/Payouts were all confirmed — by reading their actual code, not assumption — to already route every credit through `WalletService`/`CommissionService` with real DB-level idempotency guards (unique constraints or existence checks) and needed no new check. Logged one new risk-register item (23): `referrals`'/`performance_campaign_participants`' `booking_id` cascade-delete is real schema but currently unreachable, since no code path anywhere hard-deletes a completed booking. 12 new tests: `tests/Feature/Operations/ReconciliationServiceTest.php` (new — first-ever coverage for all 5 checks, not just the 2 Phase 15 added) + 2 added to `tests/Feature/Loyalty/LoyaltyServiceTest.php` for the race fix's boundary behavior (PHPUnit is single-threaded, so these verify the fix didn't change `redeem()`'s normal-case behavior/boundary, not true concurrency — same honest limitation `ServiceMatchingJobRaceTest`'s own docblock already states for its race). Full suite 583/583.
14. **API/security/E2E hardening sweep (mission Phase 16).** Read every API controller under `app/Http/Controllers/API/` against its actual route in `routes/api.php` (17 controllers, 40+ endpoints) for real IDOR/authorization gaps rather than guessing at candidates. Found the API layer itself already well-guarded — every controller re-checks ownership server-side (booking participancy, payment-account `user_id`, subscription `subscribable`, worker `assigned_worker_id`), documented in its own docblock, none trusting a route-param id alone. The two real gaps found were structural, not per-endpoint: (1) **zero general-purpose API rate limiting existed anywhere** — `bootstrap/app.php` never called `throttleApi()`, and no `RateLimiter::for('api', ...)` was registered; only the 6 explicit auth/OTP/QR routes had their own throttle, meaning every other authenticated route (wallet top-up, loyalty redeem, payment order creation, chat, subscriptions) could be called at unlimited volume by any valid Sanctum token. Fixed with Laravel's own standard starter-kit default (60/min, keyed per authenticated user or per IP for guests) via a new `RateLimiter::for('api', ...)` in `AppServiceProvider::boot()` + `$middleware->throttleApi()` in `bootstrap/app.php` — an honest engineering default, not an invented business number. (2) **9 of the pre-existing 24 business-logic API routes (WalletController::show, LoyaltyController, ProviderDiscoveryController, PlanController, SubscriptionController's 7 endpoints, PartnerWorkerController) had zero HTTP-level test coverage** — the implementation was correct on inspection but unproven at the layer a real mobile client actually calls, exactly the gap `PROJECT_CURRENT_STATE.md` §24 P4 already named. Closed with 6 new HTTP-level test files (`WalletApiTest`, `LoyaltyApiTest`, `ProviderDiscoveryApiTest`, `PlanApiTest`, `SubscriptionApiTest`, `PartnerWorkerApiTest`), each proving auth-required, IDOR rejection (one user's request can never read/mutate another's wallet/points/subscription), and the happy path — plus `ApiRateLimitTest` proving the new limiter is actually wired onto a real, previously-unthrottled route (429 past 60 req/min, scoped per-user not globally). Investigated and deliberately NOT built: Sanctum token expiration/revoke-list (a genuine mobile-session-lifetime product decision entangled with a real but bounded feature gap — logged as risk register item 24) and verifying production's real `APP_DEBUG` value (requires live production access this session didn't have/use — logged as risk register item 25, the single highest-severity item in the register if that value turns out to be `true`). Browser-driven E2E (`Playwright`/Dusk-style multi-actor journeys) remains explicitly out of scope for this phase, same as every prior mention in `PROJECT_CURRENT_STATE.md` §24 P8 — not silently dropped, just not attempted here. 33 new tests. Full suite 616/616.
15. **Multi-country/international readiness audit (mission Phase 17).** Audit-only phase — no code changes. Checked currency, timezone, and phone-number handling against real usage across the codebase rather than assuming gaps existed. Phone validation is already country-agnostic (`['required', 'string', 'max:20']`, no hardcoded regional format) — no gap. Found two real, evidence-based gaps, both logged rather than fixed in the same pass given their size and the lack of existing test coverage over display strings: (1) `Setting::get('locale.currency_symbol', '₹', $scope)` is a real, already-built, admin-editable, scope-cascaded Setting — but `DocumentService` (invoices/receipts) is its only consumer; ~16 other admin Blade views hardcode the literal `₹` character directly, the same "orphaned mechanism, one real consumer" pattern this mission has repeatedly found and fixed elsewhere (risk register item 26). (2) **The admin panel displays every timestamp in raw UTC, never converted to the viewer's or franchise's local timezone** — `countries.default_timezone` is correctly used by `DocumentService` for invoice numbering, but nothing else (`setTimezone`/`->tz()` appears nowhere else in `resources/views` or `app/Livewire`). Since confirmed real production data is India-based (`Asia/Kolkata`), this is a present-day usability gap, not a hypothetical future-country one — flagged as the highest-value single item in the risk register for actual day-to-day admin correctness (risk register item 27). Razorpay's India-only scope (already documented, item 9) and multi-language/locale content (already documented, item 18) were confirmed still accurate, not re-logged. No tax/GST field exists anywhere in the schema — confirmed as genuinely unbuilt, not a gap to invent, since tax regime is a legal/business decision tied to which countries are ever actually launched.
16. **Performance/scale audit (mission Phase 18).** Grepped every real `where()`/`whereIn()`/`groupBy()` call against every status-like column across `app/` (not indexed on assumption) to extend `2026_08_12_003000_add_indexes_to_bookings_table.php`'s own precedent-setting methodology to the rest of the schema — that migration only ever covered `bookings`. Found and indexed 9 real, evidence-confirmed, previously-unindexed hot columns across 8 tables (`payments (purpose, status)`, `bookings.payment_status` — real usage that postdates and invalidates the original bookings migration's own "no WHERE usage" note, added by Phase 15's `ReconciliationService` — `wallet_transactions (status, wallet_id)`, `dispatch_attempts.status`, `payouts.status`, `referrals.status`, `notification_logs.status`, `franchises.status`, `users (role, status)`); columns whose only real usage was already narrowed by an existing indexed FK (`partner_workers.status`, `booking_status_history.status`) were deliberately left alone since indexing them would have no real query-plan benefit. Found and fixed a genuine unbounded N+1: `ReconciliationService::walletBalanceMismatches()` loaded every wallet, then ran one additional `->transactions()->get()->sum()` query PER wallet — unlike its four sibling checks, none of which loop per-row queries — meaning 10,001 queries for 10,000 wallets on every single Operations/Troubleshoot page load; rewritten to one grouped SQL aggregate. Found and fixed a real request/scheduler-timeout risk: `CampaignNotification` had `use Queueable` scaffolding present but never `implements ShouldQueue`, so `CampaignService::send()`'s `foreach ($recipients as $r) { $r->notify(...) }` ran every recipient's send fully synchronously in-process — for a campaign sized at a few thousand recipients, a real risk of the triggering Livewire admin request or `DispatchDueCampaigns` scheduler tick simply timing out mid-send. Fixed by implementing `ShouldQueue`, bringing it in line with `BookingStatusNotification`/`SubscriptionStatusNotification`'s own already-established precedent. Found the exact same bug shape in `KycNotification` while investigating the first one — `KycReminderService::dispatchDue()`'s daily reminder cron already `chunkById(200, ...)`s its provider scan (never a memory risk) but still sent every notification synchronously inside each chunk — fixed the same way, lower severity (a cron overrunning its own tick, not a live request timing out) but the same class of bug. Investigated and deliberately NOT fixed in this pass: `CampaignService::send()`/`resendToFailedRecipients()` still load their entire resolved audience into memory in one `->get()` rather than chunking like `KycReminderService` does — logged as risk register item 28 rather than stacking a second behavioral change onto the same just-changed methods without its own dedicated test pass. 4 new tests (2 for the N+1 fix — a "balanced wallet not flagged" case that was missing before, plus a query-count regression test proving the fix no longer scales per-wallet; 1 each for the two `ShouldQueue` fixes, each verified to actually fail without its fix before being confirmed passing with it). Full suite 620/620.
17. **Final admin capability matrix (mission Phase 19).** Audit/documentation-only phase — no code changes. Wrote `FINAL_ADMIN_CAPABILITY_MATRIX.md`, the definitive, directly-verified (not copied-from-notes) inventory of the entire 33-screen admin surface. For every screen: read its actual `mount()` gate, diffed the sidebar's permission slug against it, and confirmed real test coverage exists for both the permission-denial and permission-success paths. **Result: zero gaps found** — every one of the 28 top-level screens' sidebar entries matches its own `mount()` gate exactly (the sidebar's own comment calls this a hand-kept, ungoverned sync, so this was a real drift risk to verify, not a formality), and all 33 screens/views (28 top-level + 5 detail/drill-down) have real automated coverage, split between the shared `ScreenViewAuthorizationTest` (27 screens) and 6 screens' own dedicated feature tests for those added after that shared file was last extended (Badges, Flash Sales, Performance Campaigns, KYC Support Requests, Operations, Payments). The document also flattens all 18 completed phases' capability tables into one domain-grouped summary and a risk-register disposition breakdown (business-decision items vs. real closeable follow-ups vs. confirmed-unreachable vs. blocked-on-access), so Phase 20's final release-readiness audit has one authoritative starting point instead of needing to re-derive it from 18 phases' own narratives. Full suite unchanged at 620/620 (no code touched this phase).
18. **Final release-readiness audit (mission Phase 20 — the mission's last phase).** Audit/documentation-only — no code changes. Wrote `FINAL_RELEASE_READINESS_AUDIT.md`, superseding the 2026-08-12 `PRODUCTION_READINESS_AUDIT.md` (baseline `9e8dd98`, verdict NOT READY) as the current verdict. Re-ran real, fresh checks rather than only summarizing prior phases: a mass-assignment (`$guarded = []`) scan across all 88 models (clean), a leftover-debug-statement scan (`dd()`/`dump()`/`var_dump()`, clean), a hardcoded-secrets pattern scan across `app/config/routes` (clean, `.env` confirmed not committed), and confirmed the full 162-migration chain still runs cleanly from absolute zero (every one of the 620 tests runs against a fresh `:memory:` SQLite build via `RefreshDatabase` — 620/620 passing IS that proof, not a separate claim). Compared point-by-point against the baseline's own 28-section structure: printing (didn't exist at baseline, now real PDF invoices/receipts since Phase 7), API HTTP-level test coverage (4/24 → 24/24 since Phase 16), and admin-panel RBAC completeness (baseline verified only 7 specific gaps + 1 HIGH fix; Phase 19 verified the whole 33-screen surface with zero gaps) are now fully closed baseline gaps. Admin UI design system, a standalone QA web frontend, browser-driven E2E, and true multi-process load/concurrency testing remain genuinely unstarted, unchanged from baseline — the same environment constraint baseline found (no separate MySQL QA database provisionable) still applies, unchanged. **Verdict: STILL NOT READY for an unconditioned full production launch**, for a narrower and mostly different set of reasons than baseline — recommends the same "proceed with mobile app development + admin UI design system in parallel" path baseline recommended, now on firmer ground, with verifying production's real `APP_DEBUG` value (risk item 25 — a five-minute SSH check this mission never had access to run) named as the single highest-priority, lowest-cost action item before any production traffic decision. Full suite unchanged at 620/620.
19. **Production `APP_DEBUG` verification (post-mission, user-directed, 2026-08-15, same day as Phase 20).** Explicit user request for a read-only production SSH check, after Phase 20 had named this as the mission's single highest-priority remaining action item. Connected `callf1207@srv1422426.hstgr.cloud` (`/home/1callfix.com/public_html/api/`) using this environment's already-configured SSH key — no new credentials were created or requested. Ran only read-only commands (`grep`, `ls`, `git log`, `git status`, `php artisan about`); nothing on the server was modified, no `.env` write, no cache clear, no service restart, no deploy. **Result: CONFIRMED ACTIVE.** `.env` has `APP_ENV=production`/`APP_DEBUG=true`; config is not cached (no `bootstrap/cache/config.php`), so there's no cache-vs-file ambiguity; Laravel's own `php artisan about` independently confirms `Debug Mode: ENABLED` at runtime. Production is at commit `ba0635a7e5878a42cd67b3cbf382440d580bcb90` ("Admin: Commissions browser") with a clean `git status` — matches every doc in this repo citing that commit, confirming no undocumented server-side drift. No secrets, credentials, or full `.env` contents were captured, printed, or retained — only the two `APP_ENV`/`APP_DEBUG` lines. Risk register item 25 updated from "unverified" to "CONFIRMED ACTIVE" with the safest minimal remediation documented (one-line `.env` edit + queue-worker restart) but **deliberately not performed** — editing live production is a separate, explicitly-authorized action, not something this same verification pass should also do unprompted. `FINAL_RELEASE_READINESS_AUDIT.md` and `FINAL_ADMIN_CAPABILITY_MATRIX.md` updated to reflect the confirmed (not suspected) finding. No code changed; full suite unaffected at 620/620.
20. **Production `APP_DEBUG` remediation (post-mission, user-directed, 2026-08-15, same day, under separate explicit authorization for the fix itself).** Item 19 above only verified the gap; this is the actual fix, committed as `ed26e48`. Recorded pre-change state (deployed commit `ba0635a7e5878a42cd67b3cbf382440d580bcb90`, clean server-side `git status`), backed up `.env` in place (`cp -p`, `chmod 600`, no content printed) before any edit, then changed **only** `APP_DEBUG=true` → `APP_DEBUG=false` via an anchored `sed` — confirmed via `diff` against the backup that exactly one line changed (72 lines before and after). No `config:clear`/`config:cache` needed (config was never cached). `callf1207` has no `supervisorctl`/`sudo` access (confirmed, permission denied), so the long-running Supervisor-managed queue workers were restarted via Laravel's own `php artisan queue:restart` instead — verified via `ps` that the app's own workers relaunched with fresh PIDs/near-zero uptime. One incidental, correctly-untouched finding: the server also runs queue workers for an unrelated, much older codebase at a sibling path (dated 2021) — `queue:restart`'s signal only reaches the `api/` app's own cache, so this was never at risk. Verified the fix three independent ways: `.env` re-read (`APP_DEBUG=false`), `php artisan about` (`Debug Mode: OFF`, effective runtime), and a live HTTP request to a nonexistent API route returning a clean `{"message": ...}` 404 with no `exception`/`file`/`line`/`trace` fields — real empirical proof via an actual request, not just config inspection. Deployed commit unchanged; no application code was deployed or modified, only the runtime `.env`. Risk register item 25 reclassified from "CONFIRMED ACTIVE" to "REMEDIATED/RESOLVED" — the register's first resolved item. Full detail/trail in `KNOWN_RISKS_AND_DECISIONS.md` item 25. No code changed; full suite unaffected at 620/620. **(This entry itself was added as a correction — Phase 21 item DOC-1, contradiction C1: this document had no commit-log entry for the remediation at all until this correction, and its capability-table rows still read "CONFIRMED ACTIVE" after the fix had already landed. See `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md` §0.)**
21. **`dbe6f4e` — Phase 21 item DOC-1 (documentation reconciliation).** Docs-only, no code changes. Corrected the five contradictions `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md` §0 found between the audit documents: this document's own stale "CONFIRMED ACTIVE" `APP_DEBUG` rows and missing commit-log entry for the remediation (C1, entry 20 above); `KNOWN_RISKS_AND_DECISIONS.md` items 4 and 5's stale "not built" framing for the Performance/Growth Campaign and Tips/Compensation engines, both of which have existed since mission Phases 1 and 5 respectively (C2, C3); item 12's stale "not built yet" framing for the Flash Sale/Badge engines, which predate this mission's own baseline (C4); and item 26's file-count discrepancy ("roughly 16" corrected to the actual 14, per a fresh repo-wide grep) (C5). No business/product decisions were made or implied by these corrections — only prose describing what already exists.
22. **`cfb1fa6` — Phase 21 item TECH-1 (`Payouts\Manage` row-level franchise scope).** Closed risk register item 22: `Payouts\Manage::render()` and every write action (`markProcessing()`, `confirmMarkPaid()`, `markFailed()`) now apply `AuthorizationService::scopeQuery()`/re-check the target payout's own resolved scope before acting; `PayoutsExport` takes the acting viewer and scopes identically. 9 new regression tests, verified via `git stash` to genuinely fail without the fix. Full suite: 629/629, 1494 assertions.
23. **`4c1db7c` — Phase 21 item TECH-2 (currency symbol display consistency).** Closed risk register item 26: all 14 confirmed admin Blade views now read `Setting::get('locale.currency_symbol', ...)` at each screen's own correct scope instead of a hardcoded `₹`, matching `DocumentService`'s existing invoice/receipt behavior. 16 new regression tests using a distinctive `¥` symbol to prove the Setting is actually read. Display-consistency fix only — not a claim of full multi-currency readiness. Full suite: 645/645, 1515 assertions.
24. **`ba4f72e` — Phase 21 item TECH-3 (admin panel local-timezone display).** Closed risk register item 27 for franchise-scoped operational screens: new `app/Services/TimezoneResolver.php` converts each row's own `franchise → country → default_timezone` before display (Bookings, Payouts, Commissions, Wallet Ledger, Customers, Payments, Providers, Subscriptions, Workers, Loyalty, Flash Sales, Banners, KYC Support Requests, and Operations/Health's dispatch/stuck-booking timestamps), falling back to `config('app.timezone')` when no franchise/country applies. Deliberately still UTC (franchise-agnostic system/audit data): `failed_jobs`, `scheduled_task_runs`, `payment_webhook_logs`, `NotificationLog.sent_at`. Deferred, not decided: `PerformanceCampaign.starts_at`/`ends_at`, `FlashSale.starts_at`/`ends_at`, `BadgeAssignment.expires_at`, `NotificationMeeting.starts_at` — found during this pass, left untouched pending their own policy decision. 23 new regression tests including a UTC-midnight boundary case and 2 N+1 guards. Full suite: 668/668, 1546 assertions.
25. **`e431667` — Phase 21 item TECH-4 (read-only admin chat viewer, Option A only).** Closed risk register item 15's read-only-viewing half: new `chat.view` permission (super_admin only by default), `App\Livewire\Chat\Manage` (`/admin/chat`), row-level franchise/zone scope, independently re-authorized attachment retrieval (`ChatAttachmentController`), and an `ActivityLog` entry per conversation view. **No moderation/intervention capability was built** — no send/edit/hide/delete/block/flag/report/suspend action, no admin-authored message, no `chat.moderate` permission, no schema change. That remains a genuinely open business/scope decision, tracked separately in `KNOWN_RISKS_AND_DECISIONS.md` item 15. 19 new regression tests; existing customer/partner/worker `ChatApiTest` (16 tests) unchanged and still green. Full suite: 687/687, 1578 assertions.
26. **`38eb156` — Phase 21 documentation synchronization (DOC-1/TECH-1 through TECH-4).** Docs-only, no code changes. Reconciled `KNOWN_RISKS_AND_DECISIONS.md` (items 15, 22, 26, 27), `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md`, this document (commit-log entries 21–25 and four capability-table rows), `FINAL_RELEASE_READINESS_AUDIT.md`, `FINAL_ADMIN_CAPABILITY_MATRIX.md`, and `EXACT_NEXT_TASK.md` against the actual committed implementations, with historical baseline findings preserved (strikethrough + resolution note, not rewritten) throughout. Full suite unaffected: 687/687, 1578 assertions.
27. **`df4e186` — Phase 21 item TECH-7 (`CampaignService` audience chunking).** Closed risk register item 28: `send()` and `resendToFailedRecipients()` both rewritten to `chunkById(200, ...)`, matching `KycReminderService::dispatchDue()`'s own established pattern, instead of hydrating the entire resolved audience into memory in one `->get()`. No behavior change to callers. 3 new regression tests, including a query-count assertion (205 recipients crossing the 200-row chunk boundary → exactly 2 SELECTs against `users`, not 1), verified via `git stash` to genuinely fail without the fix. **New finding discovered as a side effect (not fixed this pass):** a read-only verification for backlog item BD-12 found `FlashSaleService::priceFor()`/`::redeem()` are never actually called from the real booking-creation path (`CreateBookingAction` only applies the Plan Engine's own entitlement discount) — Flash Sale discounts currently have no effect on any real booking. Logged as `KNOWN_RISKS_AND_DECISIONS.md` item 29; not fixed, since doing so requires item 12's still-open Flash-Sale-vs-Plan stacking decision first. Full suite: 690/690, 1585 assertions.
28. **`778b60a` — Phase 21 item TECH-6 (Admin UI design system, first bounded increment — genuinely NOT complete).** TECH-6 was explicitly scoped in the backlog as a large, multi-week effort with no single completion bar ("this backlog does not attempt to define a completion bar for a multi-week UI rebuild") — this ships one honest, bounded, tested first increment, not a claim of finishing it. Built a real shared component library at `resources/views/components/ui/*.blade.php`: `x-ui.button` (primary/secondary/danger/ghost variants, sm/md/lg sizes, renders `<a>` instead of `<button>` when `href` is given), `x-ui.card` (the `bg-white rounded-lg shadow-sm` shell + optional title), `x-ui.table` (the `bg-white rounded-lg shadow-sm overflow-hidden` + `table w-full text-sm` shell, optional named `header`/`footer` slots), `x-ui.badge` (the five-color green/red/amber/blue/slate/gray status-pill palette already in ad hoc use everywhere, sm/lg sizes), and `x-ui.modal` (no modal exists anywhere in the admin panel today — kept dependency-free, `$show` is a plain Livewire boolean property wrapped in a Blade `@if`, no Alpine, matching every other conditional block already in this codebase). All five are built entirely from the Tailwind utility classes/color palette already in use across the real screens (confirmed by reading the existing 7 one-off components and a representative sample of screens first) — no new CSS framework, no build-tool dependency added. Migrated 7 screens onto the new components as a genuine proof-of-viability, chosen for being simple/representative rather than the most complex: `Providers\Index`, `Workers\Index`, `Customers\Index`, `Customers\Show`, `Commissions\Index`, `WalletLedger\Index`, `Payments\Index`. **28 of the 35 real Livewire admin screens remain on the original hand-copied Tailwind classes, unmigrated** — full list in `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md`'s TECH-6 entry. 24 new regression tests: `tests/Feature/Ui/DesignSystemComponentsTest.php` (17 tests, each primitive rendered directly) and `tests/Feature/Ui/AdminScreensDesignSystemMigrationTest.php` (7 tests, one per migrated screen, proving both correct rendering and that each screen's key interaction — a status filter, a search filter, the suspend/reactivate toggle, the export button's wiring — still reaches the same real Livewire method as before the migration). Existing `ScreenViewAuthorizationTest`, `RowLevelScopeAuthorizationTest`, `CurrencySymbolDisplayTest`, and `TimezoneDisplayTest` all continue to pass unmodified against the migrated markup, since they assert on real data appearing rather than exact class strings. Full suite: 714/714, 1656 assertions.
29. **`31e4f3b` — Phase 21 item TECH-6 (Admin UI design system, second increment — still genuinely NOT complete).** Continues the first increment's own explicit recommendation ("continue migrating screens in further bounded increments, simplest/most-representative first"). Migrated 10 more screens onto the existing `x-ui.*` components: `FranchisePricing\Manage`, `Kyc\SupportRequests`, `Chat\Manage`, `Subscriptions\Index`, `Dashboard`, `Geography\Manage`, `Workers\Show`, `Loyalty\Index`, `Providers\Show`, `Payouts\Manage` — **17/35 real Livewire screens now migrated, 18 remain** (full list in `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md`'s TECH-6 entry). `x-ui.button` extended additively (no existing call site changed behavior): a new `success` variant (green solid, for Approve-shaped actions on `Providers\Show`/`Workers\Show`) and an optional `color` prop on the `ghost` variant (red/green/gray, defaulting to the original fixed blue — several new screens use the identical underline-text-link shape for non-blue row actions: `Geography\Manage`'s red "Delete", `Loyalty\Index`'s red "Flag as fraud", `Payouts\Manage`'s green "Verify"/red "Mark Failed"). **`x-ui.modal` wired into a real screen for the first time** — `Payouts\Manage`'s "Mark Paid" flow, previously an inline table-row input with no cancel affordance at all, now a real modal using the exact same Livewire method/property names (`startMarkPaid`/`confirmMarkPaid`/`gatewayRefInput`/`markingPaidId`) plus one new, additive `cancelMarkPaid()` method for the modal's close affordance (backdrop/X button and a new footer Cancel button) — a genuine small UX improvement, not a redesign of the confirm flow. 19 new regression tests: 11 in `AdminScreensDesignSystemMigrationTest.php` (one per newly migrated screen, including a dedicated Cancel-button test for the new modal), 2 in `DesignSystemComponentsTest.php` for the button extension, plus the modal cancel path's own coverage. Existing `RowLevelScopeAuthorizationTest`'s direct-method-call coverage of the Payouts Mark Paid flow needed no changes. Full suite: 714/714 → 726/726, 1694 assertions, zero regressions.
30. **`9011ec4` — Phase 21 item TECH-6 (Admin UI design system, third increment — still genuinely NOT complete).** Continues the second increment's own explicit recommendation (smaller/mid-size screens before the largest ones). Migrated 10 more screens onto the existing `x-ui.*` components: `Auth\Login`, `Badges\Manage`, `Bookings\Show`, `Roles\Manage`, `Cms\Manage`, `Plans\Manage`, `Bookings\Index`, `Operations\Health`, `PerformanceCampaigns\Manage`, `FlashSales\Manage` — **27/35 real Livewire screens now migrated, 8 remain** (full list in `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md`'s TECH-6 entry). `x-ui.badge` extended additively: `purple` and `emerald` (solid, not a light pill) colors, both centralizing what `PerformanceCampaigns\Manage`'s (`approved`/`rewarded`) and `FlashSales\Manage`'s ("Currently active") statuses already hand-wrote. `x-ui.button` extended additively: `amber`/`purple`/`slate` added to the ghost variant's color options, for the same two screens' lifecycle-action text links. **`x-ui.modal` wired into a real screen for the second time** — `Cms\Manage`'s four dialogs (Edit Page, Edit FAQ, Delete Page, Delete FAQ), previously hand-rolled markup nearly identical to the shared modal component, now converted with the exact same Livewire method/property names throughout — no backend behavior changed. `Plans\Manage`'s and `PerformanceCampaigns\Manage`'s nested per-row detail tables (entitlements, campaign participants) deliberately left as plain HTML, matching `Geography\Manage`'s own established nested-table precedent; `Roles\Manage`'s one-off blue "Create" staff-account button deliberately left as plain HTML rather than forcing a new variant onto a single call site. 13 new regression tests: 11 in `AdminScreensDesignSystemMigrationTest.php` (one per newly migrated screen, including a dedicated edit-modal test and a dedicated delete-modal test for `Cms\Manage`), 2 in `DesignSystemComponentsTest.php` for the badge/button extensions. Full suite: 726/726 → 739/739, 1741 assertions, zero regressions.
31. **`6d76d01` — Phase 21 item TECH-6 (Admin UI design system, fourth increment — FINAL, closes TECH-6 at 35/35).** Migrated the last 8 screens onto the existing `x-ui.*` components: `Categories\Manage`, `Subcategories\Manage`, `Zones\Manage`, `Franchises\Manage`, `Services\Manage`, `Banners\Manage`, `NotificationCenter\Manage`, and `Settings\Manage` (856 lines, the largest screen in the app, done last per the third increment's own recommendation, once warmed up on the other 7). **35/35 real Livewire screens now migrated; 0 remain.** Every screen's Add-New panel is now `x-ui.card`, every status pill is `x-ui.badge`, and every confirm/edit dialog (view/edit/delete on Zones/Franchises/Services/Banners, edit/delete on Categories/Subcategories) is `x-ui.modal` — `x-ui.modal` is now wired into 8 real screens total. `Settings\Manage`'s scope-picker card and its single tab-content wrapper both moved to `x-ui.card`; all 17 "Save ... Settings" buttons across its real tabs became `x-ui.button` via one mechanical, verified regex substitution (identical class string on every one, only `wire:click` target and label differ — match count confirmed via grep before and after). **Deliberately left as plain HTML**, matching the restraint already shown in prior increments: the main list `<table>` on `Zones\Manage`/`Franchises\Manage`/`Services\Manage`/`Banners\Manage` (all four need `overflow-x-auto` for horizontal scroll on wide tables, which would conflict with `x-ui.table`'s fixed `overflow-hidden` shell), and the compact square icon action buttons (Edit/Toggle/Delete) shared identically across all 6 catalog-style screens (a shape `x-ui.button` was never designed for). `x-ui.modal` extended additively: a new `3xl` `maxWidth` option for `Zones\Manage`'s Edit Zone dialog (2-column form + boundary map side by side, needs more room than `2xl`). 8 new regression tests in `AdminScreensDesignSystemMigrationTest.php`, one per migrated screen, each proving real data still renders and a key interaction (toggle-active, toggle-status, cancel-campaign, or a real `saveDispatch()` call writing a real `Setting` row) still reaches the same Livewire method as before the migration. Full suite: 739/739 → **747/747, 1759 assertions**, zero regressions. **TECH-6 is now ✅ COMPLETE — the first time this item can be marked complete rather than started, per `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md`'s own entry.**
32. **`1348057` — Phase 21 item BD-8 (real SMS/push provider adapters — technical preparation, no vendor bound).** Read the Glover 1.8.5 reference source directly (not the prior Phase 13 audit's own summary) and established a real Provider Responsibility Map: Glover's `otpGateway` setting selects between two structurally different mechanisms (Firebase Phone Authentication, client-driven with its own SMS delivery and server-side ID-token verification, OR Glover's own server-generated/verified `OTPController` backed by a further-selectable SMS gateway) — the real 1CallFix 1.8.10 dump had `otpGateway='firebase'`, meaning Firebase Phone Auth was the mechanism actually selected for OTP in the one real prior deployment, not MSG91/GatewayAPI (their `sms_gateways.is_active=1` rows reflect Glover's admin gateway registry, not the live-selected channel) — a genuine refinement of the prior audit's "Firebase+MSG91+GatewayAPI+FCM all active" framing, not a contradiction of its underlying facts. Concluded Firebase Auth/Phone-Authentication is NOT reusable here (structurally incompatible with this app's already-complete server-side `OtpService`, which the mission forbids replacing without a proven defect — none found; Firebase also has no generic arbitrary-SMS-send product) while Firebase FCM (push only) IS reusable (purely additive, no conflict). Built `Msg91SmsAdapter` and `GatewayApiSmsAdapter` (`App\Contracts\SmsAdapter`) symmetrically — neither auto-selected, per the mission's explicit instruction — and `FirebaseFcmPushAdapter` (`App\Contracts\PushAdapter`, real FCM v1 HTTP API via a hand-rolled Google service-account JWT-bearer OAuth2 flow using PHP's own `openssl_sign`, no new composer dependency). New `App\Exceptions\InvalidPushTokenException` + `PushChannel` now clears a `User`'s stale `fcm_token` when the PROVIDER (not just a generic send failure) confirms a token `UNREGISTERED`/`NOT_FOUND`. `AppServiceProvider::register()` now resolves both adapters from `config('services.sms.driver')`/`config('services.push.driver')` (`SMS_DRIVER`/`PUSH_DRIVER` env) instead of a static bind — both default to `log`, unset in `.env.example`, so behavior in every environment that hasn't deliberately opted in is byte-for-byte unchanged. **Explicitly NOT done, and not claimed:** no real MSG91/GatewayAPI/Firebase credentials exist anywhere in this repo or environment; nothing is bound by default; no real OTP or push has been delivered to a real phone/device — BD-8 remains OPEN pending the vendor decision (MSG91 vs. GatewayAPI, neither recommended by this pass) and real credential procurement. 31 new tests, all against `Http::fake()` (zero live provider calls), including a synthetic throwaway RSA test key exercising the real JWT-signing code path without any real credential. Full suite: 747/747 → 778/778, 1759 → 1799 assertions, zero regressions.

## 3. Full backend/admin capability snapshot (this segment's additions — see `PROJECT_CURRENT_STATE.md` for everything from prior sessions)

| Capability | Status |
|---|---|
| Performance/Growth Campaign Engine | [IMPLEMENTED] [VERIFIED] |
| KYC document security + configurable requirements | [IMPLEMENTED] [VERIFIED] |
| Partner verification video | [IMPLEMENTED] [VERIFIED] |
| KYC 30-day deadline + reminders | [IMPLEMENTED] [VERIFIED] |
| Withdrawal restriction (derived, never boolean) | [IMPLEMENTED] [VERIFIED] |
| Franchise KYC support-request → Central Admin decision | [IMPLEMENTED] [VERIFIED] |
| Overtime/night/peak compensation | [IMPLEMENTED] [VERIFIED] — auto-computed |
| Rain/waiting compensation | [IMPLEMENTED] [VERIFIED] — admin-triggered (no auto data source) |
| Tips | [IMPLEMENTED] [VERIFIED] — reachable via `POST /api/bookings/{id}/tip` since Phase 11 (had zero callers before) |
| Universal Chat (Customer/Partner/Worker) | [IMPLEMENTED] [VERIFIED] — customer/partner-facing API, plus a read-only admin viewer (`/admin/chat`, `chat.view` permission) since Phase 21 item TECH-4, commit `e431667`; moderation/intervention (Option B) remains a separate open decision — see risk register item 15 |
| Printing/Document Engine (invoices/receipts, real PDF) | [IMPLEMENTED] [VERIFIED] |
| Notification Center: templates CRUD, delivery logs, provider status, retry, in-app read API | [IMPLEMENTED] [VERIFIED] |
| Payment Admin: settlement account (`payment_accounts`) self-service + admin verification | [IMPLEMENTED] [VERIFIED] |
| `payment_methods` admin UI | [BUSINESS DECISION] — blocked on risk register item 11, deliberately untouched |
| Payment webhook receipt logging + admin reprocess action | [IMPLEMENTED] [VERIFIED] |
| Scheduled-task (CRON) run-history tracking | [IMPLEMENTED] [VERIFIED] |
| Reconciliation warnings (payment/commission/wallet drift detection) | [IMPLEMENTED] [VERIFIED] — detect-and-report only, no auto-repair |
| Dispatch health (stale offers, exhausted bookings) | [IMPLEMENTED] [VERIFIED] |
| Stuck-booking detection (Setting-driven thresholds) | [IMPLEMENTED] [VERIFIED] — recovery via existing Bookings\Show actions |
| Activity/audit log wired into Operations mutations | [IMPLEMENTED] [VERIFIED] — Operations domain only, not retrofitted repo-wide |
| Admin screen view-level authorization (12 more screens: Banners, Categories, Cms, Geography, Payouts, Roles, Subcategories, Franchises, FranchisePricing, Services, Settings, Zones) | [IMPLEMENTED] [VERIFIED] |
| Sidebar visibility filtered per-permission (was unconditional for every role) | [IMPLEMENTED] [VERIFIED] |
| `settings.manage` enforced screen-wide (was 3 of 10 tabs) | [IMPLEMENTED] [VERIFIED] |
| Settings completeness: KYC / Compensation / Security-OTP / Operations / Subscriptions tabs (27 previously-unexposed real Setting keys) | [IMPLEMENTED] [VERIFIED] |
| Public content API: `GET /api/pages/{slug}`, `GET /api/faqs`, `GET /api/banners` | [IMPLEMENTED] [VERIFIED] |
| Content-page draft/published status (`content_pages.is_active`) | [IMPLEMENTED] [VERIFIED] |
| Terms & Conditions / Privacy Policy content | [BUSINESS DECISION] — blocked on legal review, risk register item 17, deliberately not fabricated |
| Multi-language / locale content support | [BUSINESS DECISION] — blocked on market-expansion decision, risk register item 18 |
| Reviews (customer submit + provider reply) | [IMPLEMENTED] [VERIFIED] — `providers.rating_avg` had been shown on the admin screen since Phase 1 with zero real data behind it |
| Glover/6amMart/1.8.10 reference comparison | [IMPLEMENTED] [VERIFIED] — see `GLOVER_6AMMART_PARITY_AUDIT.md`; real vendor-selection precedent found for SMS/push (item 8) and payment provider (item 9) |
| Master Catalog Import (Categories/Subcategories/Services) — external_id-based, idempotent, transaction-safe, audited | [IMPLEMENTED] [VERIFIED] |
| Earnings/Payouts data export (reusing existing ledger data, sensitive fields masked) | [IMPLEMENTED] [VERIFIED] |
| Vendors/Menus/Products import | [BUSINESS DECISION] — no target schema exists in 1CallFix, risk register item 20 |
| Database backup (in-app download) | [BUSINESS DECISION] — infra/ops decision, deliberately not built, risk register item 21 |
| `Payouts\Manage` row-level franchise scope | [IMPLEMENTED] [VERIFIED] — Phase 21 item TECH-1, commit `cfb1fa6`; closes risk register item 22 (list, write actions, and export all scoped) |
| Loyalty redemption race (unlocked balance check before transaction) | [IMPLEMENTED] [VERIFIED] — fixed via `lockForUpdate()` inside the transaction, matching `WalletService`'s own convention |
| Reconciliation: wallet top-ups captured without credit | [IMPLEMENTED] [VERIFIED] — detect-and-report only, no auto-repair |
| Reconciliation: negative aggregate loyalty balances | [IMPLEMENTED] [VERIFIED] — detect-and-report only, no auto-repair |
| Campaigns/Compensation/Tips/Referrals/Payouts financial-integrity audit | [VERIFIED] — all confirmed to already route through WalletService/CommissionService with real DB-level idempotency guards; no new check needed |
| `referrals`/`performance_campaign_participants` cascade-delete-on-booking-delete | [UNREACHABLE] — real schema, but no code path hard-deletes a completed booking; risk register item 23 |
| API controller IDOR/authorization audit (17 controllers, 40+ endpoints) | [VERIFIED] — every controller already re-checks ownership server-side; no new IDOR gap found |
| General-purpose API rate limiting (`throttle:api`, 60/min per user) | [IMPLEMENTED] [VERIFIED] — previously did not exist anywhere outside the 6 auth/OTP/QR routes |
| HTTP-level test coverage: Wallet/Loyalty/ProviderDiscovery/Plan/Subscription/PartnerWorker APIs | [IMPLEMENTED] [VERIFIED] — 9 routes had zero HTTP-level test before this phase |
| Sanctum token expiration / "sign out other devices" | [BUSINESS DECISION] — mobile-session-lifetime decision entangled with a real, bounded feature gap; risk register item 24 |
| Production `APP_DEBUG` value | [RESOLVED — `false`] — verified CONFIRMED ACTIVE via read-only SSH, then remediated and re-verified via a live HTTP request the same day (commit `ed26e48`); risk register item 25 |
| Browser-driven E2E (multi-actor journeys) | [NOT STARTED this segment] — explicitly out of scope for Phase 16, same as every prior mention in `PROJECT_CURRENT_STATE.md` §24 P8 |
| Currency symbol display consistency (14 admin views now read `locale.currency_symbol` instead of hardcoding ₹) | [IMPLEMENTED] [VERIFIED] — Phase 21 item TECH-2, commit `4c1db7c`; closes risk register item 26 |
| Admin panel local-timezone display (franchise-scoped operational screens now convert via `TimezoneResolver`) | [IMPLEMENTED] [VERIFIED] — Phase 21 item TECH-3, commit `ba4f72e`; closes risk register item 27 for the screens listed there. System/audit timestamps (`failed_jobs`, `scheduled_task_runs`, `payment_webhook_logs`, `NotificationLog.sent_at`) intentionally still UTC. 4 deferred scope-shaped timestamps (`PerformanceCampaign`/`FlashSale`/`BadgeAssignment`/`NotificationMeeting`) found but not converted — no policy decided |
| DB indexes on hot status/role columns: `payments`, `bookings.payment_status`, `wallet_transactions`, `dispatch_attempts`, `payouts`, `referrals`, `notification_logs`, `franchises`, `users` (9 indexes / 8 tables) | [IMPLEMENTED] [VERIFIED] — extends `2026_08_12_003000`'s bookings-only precedent to the rest of the schema |
| `ReconciliationService::walletBalanceMismatches()` N+1 (one query per wallet) | [IMPLEMENTED] [VERIFIED] — rewritten to one grouped SQL aggregate |
| `CampaignNotification`/`KycNotification` bulk-send queueing (`ShouldQueue`) | [IMPLEMENTED] [VERIFIED] — both had `use Queueable` scaffolding present but never activated; brings them in line with `BookingStatusNotification`/`SubscriptionStatusNotification`'s own precedent |
| `CampaignService::send()`/`resendToFailedRecipients()` audience chunking | [MISSING] — real, low-severity-today follow-up; risk register item 28 |
| Admin screen RBAC/sidebar/test-coverage end-to-end verification (33 screens/views) | [VERIFIED] — `FINAL_ADMIN_CAPABILITY_MATRIX.md`; zero gaps found across the whole admin surface |
| Final release-readiness audit | [IMPLEMENTED] [VERIFIED] — `FINAL_RELEASE_READINESS_AUDIT.md`; supersedes `PRODUCTION_READINESS_AUDIT.md`; verdict STILL NOT READY (Admin UI design system / browser E2E / load testing remain unstarted), but for a narrower set of reasons than baseline |
| Admin UI design system | [IMPLEMENTED] [VERIFIED] — ✅ COMPLETE, Phase 21 item TECH-6, four bounded increments, commits `778b60a` + `31e4f3b` + `9011ec4` + `6d76d01`: 6 shared `x-ui.*` primitives (button/card/table/badge/modal, modal extended with a `3xl` size) exist, `x-ui.modal` wired into 8 real screens, and **all 35/35 real Livewire admin screens are migrated onto them — 0 remain.** The main list table on 4 wide catalog screens (Zones/Franchises/Services/Banners) and the compact square icon action buttons shared across 6 catalog screens stay plain HTML by deliberate, documented restraint (see `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md`'s TECH-6 entry) |
| Browser-driven E2E / load-concurrency testing | [MISSING] — unchanged, same environment constraint baseline found |
| Production `APP_DEBUG` value | [RESOLVED — `false`] — was the highest-priority open item; one-line fix performed and verified same day (commit `ed26e48`); risk register item 25 is now the register's first resolved item |

## 4. Do not confuse these

- All commits through Phase 20 are **COMMITTED** to local `main` and **TEST-VERIFIED** (full suite run before each; Phases 17/19/20 changed no code, so their "verification" is the full suite staying green, not new tests).
- Nothing this segment is **PUSHED** or **DEPLOYED** as application code — production's deployed commit remains `ba0635a`, unchanged and untouched throughout. Production WAS, however, **inspected and then changed once, deliberately and narrowly** (items 19–20 above, post-mission, user-directed, each under its own separate explicit authorization): a read-only SSH check first confirmed both the deployed commit and the live `APP_DEBUG` value (`true`, CONFIRMED ACTIVE), then a second, separately-authorized action changed exactly one `.env` line (`APP_DEBUG=true` → `false`) and restarted the queue workers via `php artisan queue:restart` — verified via a live HTTP request that no debug information leaks. No application code was deployed or modified either time; risk register item 25 is now RESOLVED, the register's first resolved item.
- **The mission's own 20-phase priority order is now fully complete.** This does NOT mean the product is production-ready — see `FINAL_RELEASE_READINESS_AUDIT.md`'s verdict (STILL NOT READY, specific reasons named) — it means every phase this mission's own brief named has been attempted and reported on honestly, including the phases whose honest finding was "real work remains, logged, not invented as done."
