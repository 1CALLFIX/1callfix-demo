# Exact Next Task

**Current HEAD:** Phase 13 (Glover/6amMart parity audit) now complete. Found that real Glover/6amMart reference material — and, more significantly, a real prior-version 1CallFix production database (`DB_1cal_app_1.8.10.sql`) — actually exists on disk outside this git repo, correcting an earlier session's `GLOVER_VS_1CALLFIX_AUTH_AUDIT.md`, which had found nothing accessible. Full comparison in the new `GLOVER_6AMMART_PARITY_AUDIT.md`. Most open `KNOWN_RISKS_AND_DECISIONS.md` business-decision items were confirmed unresolved everywhere, including in the real 1.8.10 deployment — genuine standing decisions, not something precedent could settle. Three items got real evidence added (not code-default changes): SMS/push vendor (item 8 — Firebase+MSG91 was the real prior choice), payment provider (item 9 — Razorpay-only confirmed as deliberate precedent), and the `payment_methods` consolidation question (item 11). Closed one concrete orphaned-code gap matching the established pattern: `reviews` had real schema/model/observer since Phase 1 with zero write path anywhere — added `ReviewService`/`ReviewController` (`POST /api/bookings/{id}/review`, `POST /api/reviews/{id}/reply`) and admin visibility on `Providers\Show`. 9 new tests. Full suite: **532/532, 1161 assertions.**

**Phase 12 (CMS/content audit)** — `Cms\Manage` (Pages/FAQs) and `Banners\Manage` were both fully admin-manageable but had zero consumer anywhere in the codebase (confirmed by a full-codebase read-site grep — zero matches in routes/api.php or routes/web.php). Added a real public read API, unauthenticated by design: `GET /api/pages/{slug}`, `GET /api/faqs` (optional `?category=`), `GET /api/banners?placement=` (reuses `Banner::scopeForSlot()` — its own docblock already named "customer app API" as an intended caller). `content_pages` gained an `is_active` column matching sibling table `faqs`, plus a matching Published/Draft toggle in the admin screen — a page previously went live the instant it was saved, harmless until a public consumer existed, consequential now that one does. Investigated and deliberately NOT built: real Terms & Conditions/Privacy Policy content (a legal/business decision, not an engineering gap — fabricating legal text would be worse than an honest gap) and multi-language/locale support (confirmed single-locale throughout the entire content layer, `users.preferred_language` is dead schema — an architecture-wide decision). Both logged as new risk-register items (17, 18) rather than invented or ignored; item 19 notes a narrow, currently-inert image-orphan edge case on soft-deleted `Service` rows. 18 new tests. Full suite: **523/523, 1143 assertions.**

**Phase 11 (Admin Menu/Settings completeness audit)** — A full forensic sweep of every admin route/sidebar-item/permission/Setting key found and closed six real gaps: (1) 12 Livewire screens (Banners, Categories, Cms, Geography, Payouts, Roles, Subcategories, Franchises, FranchisePricing, Services, Settings, Zones) had NO view-level permission check — any admin-panel actor of any role could browse Payouts and the Roles & Permissions RBAC matrix; fixed by gating each `mount()` with its own existing `.manage` permission. (2) The sidebar rendered all 26 items for every role regardless of access; now filtered per-item by permission, plus Categories/Subcategories promoted to top-level entries. (3) `settings.manage` was enforced on only 3 of 10 Settings tabs; the new mount()-level gate covers all ten. (4) 27 real Setting keys (KYC/Compensation/Auth-OTP/Operations-thresholds/referral-expiry/subscription-grace-period) were read by already-wired code with zero admin UI — 5 new/expanded Settings tabs added, three of them (Security/OTP, Operations, Subscriptions) deliberately global-only since their consumers pass no scope argument at all. (5) `TipService` (built Phase 5) had zero callers anywhere — added `POST /api/bookings/{id}/tip`. (6) Corrected a doc/reality mismatch (Universal Chat had no admin screen despite being listed as an admin capability) and flagged two new risk-register items (15: no admin chat-moderation screen; 16: `partner.workers.assign` has no permission check, deliberately left alone pending a Partner-authorization-layer decision). 22 new/updated tests. Full suite: **505/505, 1102 assertions.**

**Phase 10 (Operations/Troubleshoot expansion)** — `/admin/operations` previously only surfaced failed jobs, notification failures, and static health checks. Added: real payment-webhook-receipt logging (`payment_webhook_logs`, every receipt persisted regardless of outcome — an invalid signature or unmatched order used to vanish into a log line with a 200 telling Razorpay to stop retrying) plus an admin-triggered reprocess action reusing the same idempotent `RazorpayWebhookHandler` the live endpoint uses; real scheduler run-history (`scheduled_task_runs` via `ScheduleRunTracker`, wired onto all 4 existing `Schedule::command()` entries — no row means "hasn't run," never a fabricated "healthy"); three read-only detection services (`ReconciliationService`, `DispatchHealthService`, `StuckBookingService`) surfaced on the same screen; and `ActivityLog` (existed since Phase P3, zero writers) now actually wired into every Operations mutation (job retry/discard, webhook reprocess). 11 new tests plus additive `PaymentWebhookLog` assertions on existing webhook tests. Full suite: **491/491, 1056 assertions.**

**Phase 9 (Payment Admin completion)** — `8d44153`. `payment_accounts` existed and was already READ by PayoutService/Payouts\Manage but had ZERO write path — added `PaymentAccountService` (create/update/setDefault/delete/verify, verification always admin-only, detail changes reset verification), a self-service API, admin verification UI on the existing Payouts screen, and closed an IDOR-adjacent gap in `PayoutService::request()` (a supplied payment_account_id is now checked to actually belong to the payee). `payment_methods` (the other table risk-register item 11 names) stays deliberately untouched — still blocked on its own consolidation decision. 22 tests. Full suite: **480/480, 1025 assertions.**

**Phase 8 (Notification/Communication Center completeness)** — `642d582`. Real gaps closed, not just documented: Templates CRUD (permission existed, zero UI), Delivery Logs browser (permission existed, zero UI beyond Operations' failure-only slice), Provider Status panel (which SMS/push adapter is bound, never exposes credentials), a real working `resendToFailedRecipients()` retry (idempotent, targets only currently-still-failing recipients), and the in-app notification read API (Laravel's own `database` channel was fully wired to write but had zero read-side anywhere). 22 tests. Full suite: **458/458, 997 assertions.**

**Phase 7 (Printing/Document Engine)** — `447ea55`. Added `barryvdh/laravel-dompdf` (no PDF library existed before; user chose this over a no-dependency HTML-print alternative). Real invoice/receipt PDFs for all three Payment purposes (booking/wallet_topup/plan_subscription), idempotent numbering, admin + customer-self-service authorization. 14 tests.

--- prior segment summary below (Phases 1-6), still accurate ---

## What was completed this segment ("FINAL EOD AUTONOMOUS COMPLETION MISSION", continuing from baseline `6b7c36e`)

1. **`d1e7995`** — Performance/Growth Campaign Engine (mission Phase 1).
2. **`ac816a8`+`3430197`** — Partner/Worker KYC completeness + verification video + 30-day withdrawal restriction + franchise support-request workflow (mission Phases 2/3/4, built as one engine since they share it).
3. **`cd174cc`** — Tips/waiting/rain/overtime/peak/night compensation (mission Phase 5).
4. **`a0b7b15`** — Universal Chat (mission Phase 6).

**Test suite progression:** 306/306 → 340/340 → 384/384 → 406/406 → 422/422. Full suite green before every single commit.

## Real forensic findings that shaped scope (documented, not silently worked around)

- **Admin panel exposed KYC documents via a raw, unauthenticated `<a href="{{ $doc->file_url }}">`.** Fixed — retrieval now exclusively through an authorization-gated controller.
- **`users.role` enum has no `field_worker` value** — FieldWorker-linked users use `role='provider'` (confirmed via `QaSeeder`'s own established convention), not a new enum value.
- **Waiting/rain compensation have no real auto-derivable data source** (no arrival-timestamp capture, no weather integration) — built admin-triggered only, not faked from unrelated data.
- **The 30-day KYC deadline/withdrawal-restriction text is Partner-specific throughout the mission brief** — never extended to FieldWorker; logged as risk register item 13.
- **`chat_messages` had zero consumers anywhere** — fully dormant since P2, confirmed before building the whole engine on top of it.

## What remains (honest, per the mission's own 20-phase priority order)

**Not started this segment:**
- Phase 14 — QA/realistic data expansion (badges, flash sales, campaigns, KYC states, compensation, chat). Note: `GLOVER_6AMMART_PARITY_AUDIT.md` (Phase 13) found real candidate seed data for this phase — a 140-row real 1CallFix service catalog with real ₹ pricing, real notification copy, and real zone geometry, all in the 1.8.10 dump — not imported in Phase 13, earmarked here.
- Phase 15 — Financial reconciliation audit incorporating campaign/compensation/tip paths.
- Phase 16 — API/security/E2E hardening sweep (systematic IDOR beyond what this segment's own new engines already covered for themselves).
- Phase 17 — Multi-country/international readiness audit.
- Phase 18 — Performance/scale audit.
- Phase 19 — Final admin capability matrix.
- Phase 20 — Final release-readiness audit.

## Exact next action (current)

Phases 7 through 13 are done. Continue in mission priority order: **Phase 14 — QA/realistic data expansion**. Then Phase 15 (financial reconciliation audit) and onward per the priority list above.
