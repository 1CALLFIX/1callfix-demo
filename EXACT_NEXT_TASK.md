# Exact Next Task

**Most recent work (2026-08-16): Phase 21 item TECH-6 — Admin UI design system, fourth and FINAL bounded increment.** Migrated the last 8 of the 35 real Livewire admin screens onto the existing `x-ui.*` component library: `Categories\Manage`, `Subcategories\Manage`, `Zones\Manage`, `Franchises\Manage`, `Services\Manage`, `Banners\Manage`, `NotificationCenter\Manage`, and `Settings\Manage` (856 lines, the largest screen in the app, done last once warmed up on the other 7, per the third increment's own recommendation). **35/35 screens now migrated; 0 remain — TECH-6 is ✅ COMPLETE for the first time**, not just started.

Every screen's Add-New panel is now `x-ui.card`, every status pill is `x-ui.badge`, and every confirm/edit dialog on the 8 screens (view/edit/delete on Zones/Franchises/Services/Banners, edit/delete on Categories/Subcategories) is `x-ui.modal` — `x-ui.modal` is now wired into 8 real screens total, not just isolated component tests. `Settings\Manage`'s scope-picker card and its single tab-content wrapper both moved to `x-ui.card`; all 17 "Save ... Settings" buttons across its real tabs became `x-ui.button` via one mechanical, verified regex substitution (identical class string on every one, only `wire:click` target and label differ). `x-ui.modal` was extended additively with a new `3xl` `maxWidth` option for `Zones\Manage`'s Edit Zone dialog (2-column form + boundary map side by side, needs more room than `2xl`) — no existing call site affected. **Deliberately left as plain HTML**, matching the restraint already shown in prior increments (Geography\Manage's nested table, Roles\Manage's one-off button): the main list `<table>` on `Zones\Manage`/`Franchises\Manage`/`Services\Manage`/`Banners\Manage` (all four genuinely need `overflow-x-auto` for horizontal scroll on wide, many-column tables, which would conflict with `x-ui.table`'s fixed `overflow-hidden` shell), and the compact square icon action buttons (Edit/Toggle/Delete) shared identically across all 6 catalog-style screens (a shape `x-ui.button` was never designed for and only these 6 screens use). 8 new tests in `tests/Feature/Ui/AdminScreensDesignSystemMigrationTest.php`, one per migrated screen, each proving real data still renders and a key interaction (toggle-active, toggle-status, cancel-campaign, or a real `saveDispatch()` call writing a real `Setting` row) still reaches the same Livewire method as before. Full suite: 739/739 → **747/747, 1759 assertions.** Commit `6d76d01`, none pushed.

**Phase 21's technical-only track is now fully closed** — every TECH-* item (TECH-1 through TECH-4, TECH-6, TECH-7) is COMPLETE. Only business-decision-gated items (BD-*), environment-blocked items (ENV-2/ENV-3), and unscoped testing initiatives (TEST-1/TEST-2) remain open in `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md`, none of which are engineering-actionable without a decision or infrastructure this session doesn't have.

**Before that (2026-08-16): Phase 21 item TECH-6 — Admin UI design system, second bounded increment.** Migrated 10 more of the 35 real Livewire admin screens onto the existing `x-ui.*` component library: `FranchisePricing\Manage`, `Kyc\SupportRequests`, `Chat\Manage`, `Subscriptions\Index`, `Dashboard`, `Geography\Manage`, `Workers\Show`, `Loyalty\Index`, `Providers\Show`, `Payouts\Manage`. 17/35 screens migrated at that point. `x-ui.button` extended additively with a `success` variant and a `color` prop on `ghost`. `x-ui.modal` wired into a real screen for the first time (`Payouts\Manage`'s "Mark Paid" flow). 19 new tests. Full suite: 714/714 → 726/726, 1694 assertions. Commit `31e4f3b`, none pushed.

**Before that (2026-08-16): Phase 21 item TECH-6 — Admin UI design system, first bounded increment.** Built the shared component library from scratch (`resources/views/components/ui/*.blade.php`: `x-ui.button`, `x-ui.card`, `x-ui.table`, `x-ui.badge`, `x-ui.modal`), based entirely on the Tailwind classes/color palette already in use, no new dependency. Migrated the first 7 of 35 real Livewire admin screens onto it (`Providers\Index`, `Workers\Index`, `Customers\Index`, `Customers\Show`, `Commissions\Index`, `WalletLedger\Index`, `Payments\Index`). 24 new tests. Full suite: 690/690 → 714/714, 1656 assertions. Commit `778b60a`, none pushed.

**Before that (2026-08-16): Phase 21 item TECH-7 — `CampaignService` audience chunking.** `send()`/`resendToFailedRecipients()` rewritten to `chunkById(200, ...)`, closing risk register item 28. 3 new tests. Full suite: 690/690, 1585 assertions. Commit `df4e186`. Side-effect finding logged as risk register item 29 (Flash Sale pricing never wired into the real booking path) — not fixed, blocked on item 12's stacking decision.

**Mission history below, for full context — the "FINAL EOD AUTONOMOUS COMPLETION MISSION" itself is COMPLETE** (all 20 of its own phases done); Phase 21 (Release Candidate Hardening Backlog) is separate, ongoing follow-on work, tracked in `PHASE_21_RELEASE_CANDIDATE_BACKLOG.md`, not in this mission's own 20-phase count. Phase 20 — Final release-readiness audit — was the last phase in the "FINAL EOD AUTONOMOUS COMPLETION MISSION"'s own 20-phase priority order, and is now done. Wrote `FINAL_RELEASE_READINESS_AUDIT.md`, superseding `PRODUCTION_READINESS_AUDIT.md` (2026-08-12 baseline, verdict NOT READY) as the current verdict. Re-ran fresh checks this phase (mass-assignment scan across all 88 models, debug-statement scan, hardcoded-secrets scan — all clean; confirmed the full 162-migration chain still runs cleanly via every test's `:memory:` SQLite rebuild) rather than only summarizing prior phases. **Verdict: STILL NOT READY for an unconditioned full production launch** — Admin UI design system, browser-driven E2E, and true load/concurrency testing remain genuinely unstarted (same as baseline) — but every backend/API/security/RBAC/financial gap baseline named as unstarted is now closed and tested: printing exists (Phase 7), API HTTP-level coverage went 4/24 → 24/24 (Phase 16) with rate limiting added, and the whole 33-screen admin RBAC surface was independently verified with zero gaps (Phase 19). Single highest-priority action item named: verify production's real `APP_DEBUG` value (risk item 25) — a five-minute SSH check this mission never had access to run. Full suite unchanged: **620/620, 1480 assertions.**

**If resuming work beyond this mission's own scope:** see `FINAL_RELEASE_READINESS_AUDIT.md` §15 for the prioritized next-steps list (verify `APP_DEBUG` first; then Admin UI design system and browser E2E/load testing as the two largest independent remaining tracks; then the 18 open business decisions in `KNOWN_RISKS_AND_DECISIONS.md`). The phase history below is kept for the full record of how this mission's 20 phases were executed.

---

**Phase 19 — Final admin capability matrix — now complete. Audit/documentation-only, no code changes. Directly verified (not copied from notes) every one of the admin panel's 33 real Livewire screens/views: each screen's own `mount()` gate was read (not grepped), each of the 28 top-level screens' sidebar entry was diffed against that gate's exact permission slug, and real test coverage was confirmed for both the permission-denial and permission-success paths (27 screens via the shared `ScreenViewAuthorizationTest`, 6 newer screens via their own dedicated feature tests). **Zero gaps found** — this is the first time the whole admin surface was verified end-to-end in one pass rather than assembled from 8 phases' own partial audits. Wrote `FINAL_ADMIN_CAPABILITY_MATRIX.md`: the full screen inventory table, a domain-grouped flattening of all 18 completed phases' capability tables, and a risk-register disposition breakdown (business decisions vs. real closeable follow-ups vs. confirmed-unreachable vs. blocked-on-access) — one authoritative starting point for Phase 20 instead of needing to re-derive it. Full suite unchanged: **620/620, 1480 assertions.**

**Phase 18 — Performance/scale audit — now complete. Extended `2026_08_12_003000_add_indexes_to_bookings_table.php`'s own evidence-based methodology (grep real WHERE/whereIn/groupBy usage per column, index only what's justified) from `bookings` to the rest of the schema. Found and indexed 9 real, previously-unindexed hot status/role columns across 8 tables (`payments`, `bookings.payment_status`, `wallet_transactions`, `dispatch_attempts`, `payouts`, `referrals`, `notification_logs`, `franchises`, `users`) — columns whose only real usage was already narrowed by an existing indexed FK (`partner_workers.status`, `booking_status_history.status`) were deliberately left alone. Found and fixed a genuine unbounded N+1 in `ReconciliationService::walletBalanceMismatches()` (one extra query per wallet, unlike its four sibling checks) — rewritten to a single grouped SQL aggregate. Found and fixed a real request/scheduler-timeout risk: `CampaignNotification` and `KycNotification` both had `use Queueable` scaffolding present but never `implements ShouldQueue`, so their real bulk-send loops (`CampaignService::send()`'s recipient loop; `KycReminderService::dispatchDue()`'s chunked-but-still-synchronous cron) ran every recipient's notification fully synchronously in-process — fixed both, bringing them in line with `BookingStatusNotification`/`SubscriptionStatusNotification`'s own already-established `ShouldQueue` precedent. Investigated and deliberately NOT fixed: `CampaignService::send()`/`resendToFailedRecipients()` still load their entire resolved audience into memory in one `->get()` rather than chunking like `KycReminderService` does — real but low-severity-today, logged as risk register item 28 rather than stacking a second behavioral change onto the same just-fixed methods without its own dedicated test pass. 4 new tests, each verified to actually fail without its corresponding fix. Full suite: **620/620, 1480 assertions.**

**Phase 17 — Multi-country/international readiness audit — now complete. Audit-only phase, no code changes. Checked currency, timezone, and phone-number handling against real usage rather than assuming gaps existed. Phone validation already country-agnostic (`max:20`, no hardcoded regional regex) — no gap. Found two real, evidence-based gaps, both logged rather than fixed in the same pass given their size and the lack of existing display-string test coverage: (1) `Setting::get('locale.currency_symbol', ...)` is a real, admin-editable, scope-cascaded Setting, but `DocumentService` (invoices/receipts) is its only consumer — ~16 other admin Blade views hardcode the literal ₹ character directly (risk register item 26); (2) the admin panel displays every timestamp in raw UTC, never converted to the viewer's/franchise's local time — `countries.default_timezone` is correctly used by `DocumentService` for invoice numbering but nowhere else in the codebase; since confirmed real production data is India-based (`Asia/Kolkata`), this is a present-day usability gap, not a hypothetical one — flagged as the highest-value single item in the register (item 27). Razorpay India-only scope (item 9) and multi-language content (item 18) reconfirmed accurate, not re-logged. No tax/GST field exists anywhere — confirmed genuinely unbuilt, a legal/business decision, not invented. Full suite: **616/616, 1476 assertions** (unchanged — audit only).

**Phase 16 — API/security/E2E hardening sweep — now complete. Read every API controller under `app/Http/Controllers/API/` (17 controllers, 40+ endpoints) against its real route for genuine IDOR/authorization gaps rather than guessing. Found the API layer already well-guarded — every controller re-checks ownership server-side, none trusts a route-param id alone; no new per-endpoint IDOR fix was needed. Found two real structural gaps instead: (1) no general-purpose API rate limiting existed anywhere outside the 6 auth/OTP/QR routes — fixed with Laravel's own standard 60/min-per-user default (`RateLimiter::for('api', ...)` in `AppServiceProvider` + `throttleApi()` in `bootstrap/app.php`); (2) 9 of the pre-existing 24 business-logic API routes (Wallet/Loyalty/ProviderDiscovery/Plan/Subscription/PartnerWorker) had zero HTTP-level test coverage — closed with 6 new test files plus a rate-limit regression test. Investigated and deliberately NOT built: Sanctum token expiration/revoke-list (mobile-session-lifetime product decision, risk register item 24) and production `APP_DEBUG` verification (needs live production access this session didn't use — highest-severity open item in the register if `true`, item 25). Browser-driven E2E stays explicitly out of scope, not silently dropped. 33 new tests. Full suite: **616/616, 1476 assertions.**

**Phase 15 — Financial reconciliation audit incorporating campaign/compensation/tip paths — now complete. Audited every money-moving path this mission has built (campaigns, compensation, tips, referrals, payouts, loyalty, wallet top-ups) rather than guessing at candidates. Found and fixed one genuine race: `LoyaltyService::redeem()` checked its unlocked `SUM()` balance before opening its transaction — two concurrent redemptions could both succeed, driving the aggregate ledger negative; fixed with a `lockForUpdate()` inside the transaction, matching `WalletService`'s own convention. Extended `ReconciliationService` (Phase 10) with two new read-only detection checks: wallet top-ups Razorpay captured but never credited (a real non-atomic two-step commit in `RazorpayWebhookHandler`), and negative aggregate loyalty balances (the detection half of the race above). Campaigns/Compensation/Tips/Referrals/Payouts were all confirmed to already route through WalletService/CommissionService with real DB-level idempotency guards — no new check needed. Logged one new risk-register item (23): referrals'/performance_campaign_participants' cascade-delete-on-booking-delete is real but currently unreachable. 12 new tests. Full suite: **583/583, 1292 assertions.**

**Phase 14 — redefined mid-session by explicit user direction to mean "Master Catalog Import + Operations Import/Export/Backup completeness" (the original roadmap's "QA/realistic data expansion" label for phase 14 is carried forward as still-open work, not dropped — see "What remains" below) — now complete. User supplied real historical catalog spreadsheets + real screenshots of the reference product's live `/central/operations/{imports,exports,backup}` admin screens. The existing Categories/Subcategories/Services import (built in an earlier, unlogged session) had a real working UI but zero tests and reused a source file's `id` column as the real PK — rebuilt around a new shared `App\Services\Catalog\CatalogImporter` engine with a decoupled `external_id` column, real DB-transaction rollback-on-partial-failure, and a persisted `catalog_import_runs` audit trail. Vendors/Menus/Products import and an in-app database backup feature were investigated and deliberately NOT built (no target schema for the former; a real exfiltration/ops risk to improvise for the latter) — both logged as new risk-register items (20, 21) instead. Added real Earnings/Payouts data exports reusing existing ledger data, with banking secrets masked — found and logged (not silently fixed) that `Payouts\Manage` itself has no row-level franchise scoping (item 22). 39 new tests. Full suite: **571/571, 1276 assertions.**

**Phase 13 (Glover/6amMart parity audit)** — Found that real Glover/6amMart reference material — and, more significantly, a real prior-version 1CallFix production database (`DB_1cal_app_1.8.10.sql`) — actually exists on disk outside this git repo, correcting an earlier session's `GLOVER_VS_1CALLFIX_AUTH_AUDIT.md`, which had found nothing accessible. Full comparison in the new `GLOVER_6AMMART_PARITY_AUDIT.md`. Most open `KNOWN_RISKS_AND_DECISIONS.md` business-decision items were confirmed unresolved everywhere, including in the real 1.8.10 deployment — genuine standing decisions, not something precedent could settle. Three items got real evidence added (not code-default changes): SMS/push vendor (item 8 — Firebase+MSG91 was the real prior choice), payment provider (item 9 — Razorpay-only confirmed as deliberate precedent), and the `payment_methods` consolidation question (item 11). Closed one concrete orphaned-code gap matching the established pattern: `reviews` had real schema/model/observer since Phase 1 with zero write path anywhere — added `ReviewService`/`ReviewController` (`POST /api/bookings/{id}/review`, `POST /api/reviews/{id}/reply`) and admin visibility on `Providers\Show`. 9 new tests. Full suite: **532/532, 1161 assertions.**

**Phase 12 (CMS/content audit)** — `Cms\Manage` (Pages/FAQs) and `Banners\Manage` were both fully admin-manageable but had zero consumer anywhere in the codebase (confirmed by a full-codebase read-site grep — zero matches in routes/api.php or routes/web.php). Added a real public read API, unauthenticated by design: `GET /api/pages/{slug}`, `GET /api/faqs` (optional `?category=`), `GET /api/banners?placement=` (reuses `Banner::scopeForSlot()` — its own docblock already named "customer app API" as an intended caller). `content_pages` gained an `is_active` column matching sibling table `faqs`, plus a matching Published/Draft toggle in the admin screen — a page previously went live the instant it was saved, harmless until a public consumer existed, consequential now that one does. Investigated and deliberately NOT built: real Terms & Conditions/Privacy Policy content (a legal/business decision, not an engineering gap — fabricating legal text would be worse than an honest gap) and multi-language/locale support (confirmed single-locale throughout the entire content layer, `users.preferred_language` is dead schema — an architecture-wide decision). Both logged as new risk-register items (17, 18) rather than invented or ignored; item 19 notes a narrow, currently-inert image-orphan edge case on soft-deleted `Service` rows. 18 new tests. Full suite: **523/523, 1143 assertions.**

**Phase 11 (Admin Menu/Settings completeness audit)** — A full forensic sweep of every admin route/sidebar-item/permission/Setting key found and closed six real gaps: (1) 12 Livewire screens (Banners, Categories, Cms, Geography, Payouts, Roles, Subcategories, Franchises, FranchisePricing, Services, Settings, Zones) had NO view-level permission check — any admin-panel actor of any role could browse Payouts and the Roles & Permissions RBAC matrix; fixed by gating each `mount()` with its own existing `.manage` permission. (2) The sidebar rendered all 26 items for every role regardless of access; now filtered per-item by permission, plus Categories/Subcategories promoted to top-level entries. (3) `settings.manage` was enforced on only 3 of 10 Settings tabs; the new mount()-level gate covers all ten. (4) 27 real Setting keys (KYC/Compensation/Auth-OTP/Operations-thresholds/referral-expiry/subscription-grace-period) were read by already-wired code with zero admin UI — 5 new/expanded Settings tabs added, three of them (Security/OTP, Operations, Subscriptions) deliberately global-only since their consumers pass no scope argument at all. (5) `TipService` (built Phase 5) had zero callers anywhere — added `POST /api/bookings/{id}/tip`. (6) Corrected a doc/reality mismatch (Universal Chat had no admin screen despite being listed as an admin capability) and flagged two new risk-register items (15: no admin chat-moderation screen; 16: `partner.workers.assign` has no permission check, deliberately left alone pending a Partner-authorization-layer decision). 22 new/updated tests. Full suite: **505/505, 1102 assertions.**

**Phase 10 (Operations/Troubleshoot expansion)** — `/admin/operations` previously only surfaced failed jobs, notification failures, and static health checks. Added: real payment-webhook-receipt logging (`payment_webhook_logs`, every receipt persisted regardless of outcome — an invalid signature or unmatched order used to vanish into a log line with a 200 telling Razorpay to stop retrying) plus an admin-triggered reprocess action reusing the same idempotent `RazorpayWebhookHandler` the live endpoint uses; real scheduler run-history (`scheduled_task_runs` via `ScheduleRunTracker`, wired onto all 4 existing `Schedule::command()` entries — no row means "hasn't run," never a fabricated "healthy"); three read-only detection services (`ReconciliationService`, `DispatchHealthService`, `StuckBookingService`) surfaced on the same screen; and `ActivityLog` (existed since Phase P3, zero writers) now actually wired into every Operations mutation (job retry/discard, webhook reprocess). 11 new tests plus additive `PaymentWebhookLog` assertions on existing webhook tests. Full suite: **491/491, 1056 assertions.**

**Phase 9 (Payment Admin completion)** — `8d44153`. `payment_accounts` existed and was already READ by PayoutService/Payouts\Manage but had ZERO write path — added `PaymentAccountService` (create/update/setDefault/delete/verify, verification always admin-only, detail changes reset verification), a self-service API, admin verification UI on the existing Payouts screen, and closed an IDOR-adjacent gap in `PayoutService::request()` (a supplied payment_account_id is now checked to actually belong to the payee). `payment_methods` (the other table risk-register item 11 names) stays deliberately untouched — still blocked on its own consolidation decision. 22 tests. Full suite: **480/480, 1025 assertions.**

**Phase 8 (Notification/Communication Center completeness)** — `642d582`. Real gaps closed, not just documented: Templates CRUD (permission existed, zero UI), Delivery Logs browser (permission existed, zero UI beyond Operations' failure-only slice), Provider Status panel (which SMS/push adapter is bound, never exposes credentials), a real working `resendToFailedRecipients()` retry (idempotent, targets only currently-still-failing recipients), and the in-app notification read API (Laravel's own `database` channel was fully wired to write but had zero read-side anywhere). 22 tests. Full suite: **458/458, 997 assertions.**

**Phase 7 (Printing/Document Engine)** — `447ea55`. Added `barryvdh/laravel-dompdf` (no PDF library existed before; user chose this over a no-dependency HTML-print alternative). Real invoice/receipt PDFs for all three Payment purposes (booking/wallet_topup/plan_subscription), idempotent numbering, admin + customer-self-service authorization. 14 tests.

--- prior segment summary below (Phases 1-6), still accurate ---

## What was completed this segment ("FINAL EOD AUTONOMOUS COMPLETION MISSION", continuing from baseline `6b7c36e`)

1. **`d1e7995`** — Performance/Growth Campaign Engine (mission Phase 1).
2. **`ac816a8`+`3430197`** — Partner/Worker KYC completeness + verification video + 30-day withdrawal restriction + franchise support-request workflow (mission Phases 2/3/4, built as one engine since they share it).
3. **`cd174cc`** — Tips/waiting/rain/overtime/peak/night compensation (mission Phase 5).
4. **`a0b7b15`** — Universal Chat (mission Phase 6).

**Test suite progression:** 306/306 → 340/340 → 384/384 → 406/406 → 422/422. Full suite green before every single commit.

## Real forensic findings that shaped scope (documented, not silently worked around)

- **Admin panel exposed KYC documents via a raw, unauthenticated `<a href="{{ $doc->file_url }}">`.** Fixed — retrieval now exclusively through an authorization-gated controller.
- **`users.role` enum has no `field_worker` value** — FieldWorker-linked users use `role='provider'` (confirmed via `QaSeeder`'s own established convention), not a new enum value.
- **Waiting/rain compensation have no real auto-derivable data source** (no arrival-timestamp capture, no weather integration) — built admin-triggered only, not faked from unrelated data.
- **The 30-day KYC deadline/withdrawal-restriction text is Partner-specific throughout the mission brief** — never extended to FieldWorker; logged as risk register item 13.
- **`chat_messages` had zero consumers anywhere** — fully dormant since P2, confirmed before building the whole engine on top of it.

## What remains (honest, per the mission's own 20-phase priority order)

**Not started this segment:**
- **QA/realistic data expansion** (badges, flash sales, campaigns, KYC states, compensation, chat) — the ORIGINAL roadmap content for "Phase 14," superseded in this segment's numbering by the user's mid-session redefinition (now "Master Catalog Import + Operations Import/Export completeness," done — see above), but not dropped as work: still real, still needed. `GLOVER_6AMMART_PARITY_AUDIT.md` (Phase 13) found real candidate seed data for it — a 140-row real 1CallFix service catalog with real ₹ pricing, real notification copy, and real zone geometry, all in the 1.8.10 dump — and Phase 14's new `App\Services\Catalog\CatalogImporter` is now the real, tested tool that could import it. Pick this up whenever QA data is next prioritized.
- ~~Risk register item 26 — hardcoded ₹ across ~16 admin Blade views instead of reading `locale.currency_symbol`.~~ **Resolved 2026-08-16, Phase 21 item TECH-2, commit `4c1db7c`** — all 14 confirmed views now read the Setting.
- ~~Risk register item 27 — admin panel displays raw UTC everywhere except `DocumentService`.~~ **Resolved 2026-08-16, Phase 21 item TECH-3, commit `ba4f72e`, for the franchise-scoped operational screens it names** — system/audit timestamps intentionally still UTC; 4 deferred scope-shaped timestamps (`PerformanceCampaign`/`FlashSale`/`BadgeAssignment`/`NotificationMeeting`) found but not converted, no policy decided.
- Risk register item 28 — `CampaignService::send()`/`resendToFailedRecipients()` load their entire resolved audience into memory in one `->get()` rather than chunking. Real but low-severity-today; not attempted in Phase 18's pass to avoid stacking a second behavioral change onto the same just-fixed methods. Still open (Phase 21 item TECH-7).

**Mission phases 7–20 are all complete.** All genuinely-open work beyond this mission's own scope is tracked in `KNOWN_RISKS_AND_DECISIONS.md` (28 items) and `FINAL_RELEASE_READINESS_AUDIT.md` §13/§15, not in this list.

## Exact next action (current)

None — the mission's own 20-phase priority order is complete. See `FINAL_RELEASE_READINESS_AUDIT.md` for the prioritized list of what a future session should pick up next (verify production `APP_DEBUG` first; then Admin UI design system and browser E2E/load testing as the two largest remaining tracks; then the open business decisions in the risk register).
